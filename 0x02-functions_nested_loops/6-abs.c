#include "main.h"

/**
 * _abs - It is the entry point
 * @n: number to be computed
 *  Description: computes the absolute value of an integer
 *  Return: absolute value of n
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
