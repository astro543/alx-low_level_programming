#include "main.h"
#include <stdio.h>

/**
 * add -It is entry point
 * @n: Integer to be added
 * @i: Integer to be added
 * Description: Function adds two integers
 * Return: Sum of two integers
 */

int add(int n, int i)
{
	int ret = n + i;

	return (ret);
}
